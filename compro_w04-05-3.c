// ผลลัพธ์ที่ได้ต่างกันอย่างไร
 // เพราะ static จะเก็บค่าของตัวแปรไว้ระหว่างการเรียกใช้งานฟังก์ชัน
// ความแตกต่างของ Non-Static Variable
 // เพราะ static จะเก็บไว้ในหน่วยความจำระหว่างการใช้งาน
// อภิปราย และสรุปผลที่ได้จากการทดลอง
 // เพราะ static เป็นการให้ค่าคงที่ที่การใช้ฟังก์ชัน เช่น จำนวนการเรียก